import styled from "styled-components";
import Container from "./container";
import {blockMargin} from "../../mixins";

export const MarginContainer = styled(Container)`
    ${blockMargin}
`

export default MarginContainer